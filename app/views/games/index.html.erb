<h1>Games Index</h1>

<ul>
  <% @games.each do |game| %>
    <li><%= link_to "Game #{game.id}", game_path(game) %></li>
  <% end %>
</ul>

<%= form_for(:game) do |f| %>
  <%= f.submit "New Game" %>
<% end %>
