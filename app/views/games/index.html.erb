<h1>All Games</h1>

<ul>
  <% @games.each do |game| %>
    <li><%= link_to "#{game.user.username} vs. Computer", game_path(game) %></li>
  <% end %>
</ul>

<%= form_for :game, url: games_path, method: :post do |f| %>
  <%= f.submit "New Game - Play as X", class: 'btn btn-success' %>
<% end %>

<br>

<%= form_for :game, url: games_path, method: :post do |f| %>
  <%= f.submit "New Game - Play as O", class: 'btn btn-success' %>
<% end %>
